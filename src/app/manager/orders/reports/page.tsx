import { ComingSoon } from "@/components/orders/ComingSoon";

export default function OrderReportsPage() {
  return (
    <ComingSoon
      title="Reports"
      note="Order Enquiry (in the sidebar) already lists every submitted order — dedicated report exports/breakdowns aren't built yet."
    />
  );
}
