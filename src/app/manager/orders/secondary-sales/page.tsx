import { ComingSoon } from "@/components/orders/ComingSoon";

export default function SecondarySalesReportingPage() {
  return (
    <ComingSoon
      title="Secondary Sales Reporting"
      note="Hospital-side sell-through reporting (secondary sales) isn't modeled yet — usage logging tracks consumption, not resale."
    />
  );
}
