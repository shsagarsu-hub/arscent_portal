import { ComingSoon } from "@/components/orders/ComingSoon";

export default function TemplateManagerPage() {
  return (
    <ComingSoon
      title="Template Manager"
      note="Saving a reusable order template, and the spreadsheet upload path on each order form's Add Product panel, aren't wired up yet."
    />
  );
}
